<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
  xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <uses-permission
    android:name="android.permission.FOREGROUND_SERVICE"
    tools:ignore="ForegroundServicesPolicy" />
  <uses-permission
    android:name="android.permission.FOREGROUND_SERVICE_SHORT_SERVICE"
    tools:ignore="SystemPermissionTypo" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission
    android:name="android.permission.USE_FULL_SCREEN_INTENT"
    tools:ignore="FullScreenIntentPolicy" />

  <uses-permission android:name="android.permission.VIBRATE" />

  <application
    android:name=".OneTimerApplication"
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.OneTimer">
    <activity
      android:name=".activities.main.MainActivity"
      android:exported="true"
      android:theme="@style/Theme.OneTimer"
      android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
      android:name=".activities.alarm.AlarmActivity"
      android:exported="true"
      android:label="@string/title_activity_alarm"
      android:theme="@style/Theme.OneTimer"
      android:windowSoftInputMode="adjustResize" />

    <service
      android:name=".external.alarm.AlarmService"
      android:exported="false"
      android:foregroundServiceType="shortService" />

    <receiver
      android:name=".external.alarm.AlarmBroadcastReceiver"
      android:exported="false" />

    <service
      android:name=".external.tile.AlarmTileService"
      android:exported="true"
      android:icon="@drawable/baseline_alarm_24"
      android:label="@string/tile_alarm_label"
      android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
      <intent-filter>
        <action android:name="android.service.quicksettings.action.QS_TILE" />
      </intent-filter>
    </service>
  </application>

</manifest>
